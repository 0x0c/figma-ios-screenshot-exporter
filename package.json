{
  "name": "ios-screenshot-exporter",
  "version": "1.0.0",
  "description": "Figma plugin: export iOS screenshots named by device family + size",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.128.0",
    "typescript": "^5.0.0"
  }
}
